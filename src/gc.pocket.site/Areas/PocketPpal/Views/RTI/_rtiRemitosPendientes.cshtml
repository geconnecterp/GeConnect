
@model GridCoreSmart<RemitoGenDto>
@{
    var dataMod = new RemitoGenDto();
    var alt = true;
    string clase = string.Empty;
    string sortDirInv = Model.SortDir.Equals("ASC") ? "DESC" : "ASC";
    int cont = 0;
}
<div class="grid-golden h-100">
    <div class="grid-golden-body p-0">
    <div class="table-responsive table-wrapper">
    <table class="table table-sm mb-0 table-hover table-wrapper-fixed-head table-golden" id="tbRemPend">
        <thead class="sticky-top table-golden-header-compact">
            <tr class="header">
                <th scope="col" class="th-compact" style="width:1%; display:none"></th>
                <th scope="col" class="th-compact">Remito N°</th>
                <th scope="col" class="th-compact">TR N°</th>
                <th scope="col" class="th-compact">Suc.Origen</th>
                <th scope="col" class="th-compact">Fecha</th>
                @* <th scope="col"></th> *@
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ListaDatos)
            {
                cont++;
                if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }

                <tr class="@clase" onclick="seleccionaRegistroRP(this)">
                    <td class="td-compact" style="display:none;">@cont </td>
                    <td class="td-compact" style="min-width:200px;">@item.re_compte</td>
                    <td class="td-compact">@item.ti</td>
                    <td class="td-compact corta-linea">@item.adm_nombre</td>
                    <td class="td-compact">@item.re_fecha</td>
                    @* <td><a asp-action="ResguardarAutorizacionProveedor" asp-controller="rpr" asp-area="pocketppal" class="btn btn-xs btn-secondary" asp-route-rp="@item.Rp"> <i class="bx bxs-send bx-xs"></i></a></td> *@
                </tr>
            }
            @* <tr>
            <td colspan="9">
            @Html.PagedListPager(
            (IPagedList)Model.ListaDatos,
            page => Url.Action("Index", "producto", new { page = page, area = "Personas", sort = Model.Sort, sortdir = sortDirInv }), new PagedListRenderOptions
            {
            LiElementClasses = new string[] { "page-item" },
            PageClasses = new string[] { "page-link" },
            Display = PagedListDisplayMode.IfNeeded
            })
            </td>
            </tr> *@
        </tbody>
    </table>
    </div>
    </div>
</div>
