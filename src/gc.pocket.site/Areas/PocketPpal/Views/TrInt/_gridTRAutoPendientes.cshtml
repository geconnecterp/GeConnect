@model GridCoreSmart<AutorizacionTIDto>

@{
    var dataMod = new InfoProdLP();
    var alt = true;
    string clase = string.Empty;
    string sortDirInv = Model.SortDir.Equals("ASC") ? "DESC" : "ASC";
    int cont = 0;
}
<div class="pocket-grid-scroll table-responsive table-wrapper pocket-ti-authorizations">
    <table class="pocket-grid table table-sm mb-0 table-hover table-wrapper-fixed-head" id="tbAuPend">
        <thead>
            <tr class="header table-dark">
                <th scope="col" style="width:1%"></th>
                <th scope="col">Sucursal</th>
                <th scope="col">N° Auto TI</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ListaDatos!)
            {
                cont++;
                if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }

                <tr class="@clase" onclick="seleccionarRegistroTR(this)" >
                    <td>@cont </td>
                    <td class="corta-linea">
                        @item.Adm_nombre
                        @Html.Hidden("ti"+@cont,item.Ti)
                        @Html.Hidden("usu" + cont, item.Usu_id)
                        @Html.Hidden("fec" + cont, item.Fecha.ToShortDateString())
                    </td>
                    <td class="text-nowrap">@item.Ti</td>
                </tr>
            }         
        </tbody>
    </table>
</div>
