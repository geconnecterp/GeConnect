@model Ls03Model
@{
	var ls03 = Model.ListaLs03;
}
<div class="input-group input-group-sm">
    <span class="input-group-text">Nombre</span>
    @Html.DropDownListFor(m => m.id, ls03, "Seleccionar", new { @id = "listaLs03", disabled = "disabled", @class = "form-control inputEditable activable" })
</div>