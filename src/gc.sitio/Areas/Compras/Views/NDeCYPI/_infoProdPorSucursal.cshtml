@model GridCoreSmart<gc.infraestructura.Dtos.Productos.InfoProdStkA>
@{
	var alt = true;
	string clase = string.Empty;
}
<div class="table-responsive text-nowrap table-wrapper-200-full">
	<table class="table table-sm mb-0 table-hover table-golden" id="tbInfoProdStkA">
		<thead>
			<tr class="header">
				<th style="width:10%;" class="tiny-row align-center">ID</th>
				<th style="width:40%;" class="tiny-row align-center">Sucursal</th>
				<th style="width:10%;" class="tiny-row align-center">Stk</th>
				<th style="width:20%;" class="tiny-row align-center">Proy. Vtas. Mes</th>
				<th style="width:20%;" class="tiny-row align-center">Proy. Vtas. Día</th>
				
			</tr>
		</thead>
		<tbody>
			@if (Model != null && Model.ListaDatos != null && Model.ListaDatos.Count > 0)
			{
				@foreach (var item in Model.ListaDatos)
				{
					if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }
					<tr class="@clase" onclick="selectListaInfoProdStkD(this)">
						<td class="align-center tiny-row">@item.Adm_id</td>
						<td class="align-left tiny-row">@item.Adm_nombre</td>
						<td class="align-center tiny-row">@item.ps_stk</td>
						<td class="align-center tiny-row">@item.Proy_vtas</td>
						<td class="align-center tiny-row">@item.Proy_vtas_dia</td>
					</tr>
				}
			}
		</tbody>
	</table>
</div>