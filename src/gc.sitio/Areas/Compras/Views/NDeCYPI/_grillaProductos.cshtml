@model BuscarProductosOCPI2Model;
@using static gc.infraestructura.Helpers.GridHelper
@{
	var alt = true;
	string clase = string.Empty;

	string grupoActual = string.Empty;
	string grupoAnterior = string.Empty;

	decimal totalCosto = 0;
	decimal totalPallet = 0;
}
@section styles {
	<link rel="stylesheet" href="~/css/compras/ndecypi/ndecypi.css" />
}
<style>
	.bg-pedido-m {
		background-color: lightgreen !important;
	}

	.bg-pedido-a {
		background-color: #6cc6f3 !important;
	}

	#containerListaProducto {
		position: relative;
		height: 600px;
		max-height: 600px;
		overflow-y: auto;
		overflow-x: auto;
		z-index: auto;
		background: #fff; 
	}

	.table-golden {
		border-collapse: separate;
		border-spacing: 0;
		table-layout: fixed;
	}

		.table-golden thead {
			position: sticky;
			top: 0;
			z-index: 1;
			background: linear-gradient(to bottom, #b8860b, #daa520) !important;
			color: #fff !important;
		}

			.table-golden thead th {
				position: sticky;
				top: 0;
				z-index: 4;
				background: linear-gradient(to bottom, #b8860b, #daa520);
				color: #fff;
				box-shadow: 0 1px 0 rgba(0,0,0,0.05), 0 2px 2px rgba(0,0,0,0.04);
				background-clip: padding-box;
			}

			.table-golden thead,
			.table-golden thead th {
				/*background-color: #b8860b;  tu dorado */
				/* color: #333; */
			}

	#tbListaProducto th,
	#tbListaProducto td {
		padding: 0.01rem 0.15rem !important;
		line-height: 1.2 !important;
		height: auto !important;
	}

	#tbListaProducto tr {
		height: 1.6rem !important;
	}

	.th-descripcion {
		position: relative;
		text-align: center;
		vertical-align: middle;
		width: 10%;
	}

		.th-descripcion .th-title {
			display: inline-block;
		}

	.btn-info-producto {
		position: absolute;
		right: 4px;
		top: 50%;
		transform: translateY(-50%);
		background: transparent !important;
		border: none !important;
		padding: 0;
		color: #fff; 
		cursor: pointer;
	}

		.btn-info-producto i {
			font-size: 1rem;
			line-height: 1;
		}

	#tbListaProducto tfoot {
		position: sticky;
		bottom: 0;
		z-index: 2;
	}

		#tbListaProducto tfoot .total-row {
			border-top: 2px solid #999;
			background-color: #e9ecef;
		}

	.cell-ellipsis {
		white-space: nowrap !important;
		overflow: hidden !important;
		text-overflow: ellipsis !important;
		display: flex; /* en vez de block */
		align-items: center; /* centra verticalmente el texto */
		width: 100%;
	}

	.bg-stk-verde {
		background-color: #2e7d32 !important; /* verde oscuro */
		color: #fff !important;
	}

	.bg-stk-azul {
		background-color: #1565c0 !important; /* azul */
		color: #fff !important;
	}

	.bg-stk-rojo {
		background-color: #c62828 !important; /* rojo */
		color: #fff !important;
	}
	.disc-icon {
		position: absolute;
		right: 2px;
		top: 50%;
		transform: translateY(-50%);
	}
</style>
<div class="table-responsive table-wrapper-full-width" id="containerListaProducto">
	<table class="table table-sm mb-0 table-hover table-golden" id="tbListaProducto">
		<thead>
			<tr class="header">
				<th style="width:3%;" class="align-center tiny-row">Código</th>
				<th class="th-descripcion">
					<span class="th-title">Descripción</span>
					<button type="button"
							class="btn btn-info-producto"
							data-action="info-producto"
							title="Abrir info de producto">
						<i class="bx bx-info-circle"></i>
					</button>
				</th>
				<th style="width:8%;" class="align-center">Id. Barrado</th>
				<th style="width:3%;" class="align-center tiny-row">Cod. Prov</th>
				@if (Model.Tipo == "PI")
				{
					<th style="width:2%;" class="align-center tiny-row" title="Días de Última Transferencia">Días Ult. Transf.</th>
					<th style="width:2%;" class="align-center tiny-row" title="Pedido Interno Pendiente">PI Pend</th>
				}
				else
				{
					<th style="width:2%;" class="align-center tiny-row">Días Ult. RP</th>
					<th style="width:2%;" class="align-center tiny-row">OC Pend</th>
				}
				<th style="width:2%;" class="align-center tiny-row">Stock Suc</th>
				<th style="width:3%;" class="align-center tiny-row">Stock Tot</th>
				<th style="width:3%;" class="align-center tiny-row" title="Ventas últimos 30 días">Vtas. u30</th>@**@
				<th style="width:3%;" class="align-center tiny-row" title="Nivel de Demanda 30 días">Vta/Stk u30</th>
				<th style="width:3%;" class="align-center tiny-row" title="Proyección de Ventas 30 días">Proy. 30</th>
				<th style="width:3%;" class="align-center tiny-row" title="Proyección de Ventas 60 días">Proy. 60</th>
				<th style="width:3%;" class="align-center tiny-row" title="Proyección de Ventas 90 días">Proy. 90</th>
				<th style="width:3%;" class="align-center tiny-row" title="Días de Stock">Días Stk.</th>
				<th style="width:0%; display:none;" class="align-center tiny-row">Stock a Facturar</th>
				<th style="width:2%;" class="align-center tiny-row" title="Unidad de Presentación">Uni. Pres.</th>
				<th style="width:3%;" class="align-center tiny-row" title="Unidades por Pallet">Uni. Pallet</th>
				<th style="width:3%;" class="align-center tiny-row" data-col="pedido-bultos">Pedido Bultos</th> @*18*@
				<th style="width:5%;" class="align-center tiny-row" data-col="pedido-cantidad">Pedido Cantidad</th>
				<th style="width:7%;" class="align-center tiny-row" data-col="precio-costo">Precio Costo</th>
				<th style="width:7%;" class="align-center tiny-row" data-col="total-costo">Total Costo</th>
				<th style="width:5%;" class="align-center tiny-row" data-col="total-pallet">Total Pallet</th>
				<th style="width:0%; display:none;">pg_id</th>
				<th style="width:0%; display:none;">rub_id</th>
			</tr>
		</thead>
		<tbody>
			@if (Model != null && Model.ListaDatosProductos != null && Model.ListaDatosProductos.ListaDatos != null && Model.ListaDatosProductos.ListaDatos.Count > 0)
			{
				var productos = Model.ListaDatosProductos.ListaDatos.OrderBy(x => x.cta_id).ThenBy(y => y.pg_id).ThenBy(z => z.p_orden_pg).ToList();
				
				@foreach (var item in productos)
				{
					grupoActual = item.cta_id + item.pg_id;
					if (grupoActual != grupoAnterior)
					{
						var leyenda = $"Prov: {item.cta_id} {item.cta_denominacion} Familia: {item.pg_id} {item.pg_desc}";
						<tr class="table-secondary fw-bold">
							<td colspan="21" class="text-center">
								<i class="bx bx-folder me-1"></i>
								@leyenda
							</td>
						</tr>
						grupoAnterior = grupoActual;
						alt = true; 
					}

					if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }
					<tr class="@clase"
						data-id="@item.p_id"
						data-cta-id="@item.cta_id"
						data-cta-denominacion="@item.cta_denominacion"
						data-pedido-tipo="@item.pedido_tipo"
						onclick="selectListaProductoRow(this)">
						<td class="align-center tiny-row">@item.p_id</td>
						@if (item.p_activo == "D")
						{
							<td class="align-left tiny-row" style="position:relative;">
								<span class="cell-ellipsis" title="@item.p_desc">
									@item.p_desc
								</span>
								<span class="disc-icon" title="Producto discontinuo">
									<i class="bx bx-dislike"></i>
								</span>
							</td>
						}
						else
						{
							<td class="align-left tiny-row">
								<span class="cell-ellipsis" title="@item.p_desc">
									@item.p_desc
								</span>
							</td>
						}
						<td class="align-center tiny-row">@item.p_id_barrado</td>
						<td class="align-center tiny-row">@item.p_id_prov</td>
						@if(Model.Tipo == "PI")
						{
							<td class="align-right tiny-row">@item.re_dias</td>
							<td class="align-right tiny-row" style="font-weight:bold;">@item.pi_pendiente</td>
						}
						else
						{
							<td class="align-right tiny-row">@item.rp_dias</td>
							<td class="align-right tiny-row" style="font-weight:bold;">@item.oc_pendiente</td>
						}
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.stk_suc, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.stk, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.vta_30, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>@**@
						@{
							string demanda30;
							if (item.vta_30_dias_con_stk == 0)
							{
								demanda30 = "SND";
							}
							else
							{
								var valor = Math.Round((decimal)item.vta_30_dias / (decimal)item.vta_30_dias_con_stk, 3);

								if (valor < 0.001M) valor = 0.001M;
								if (valor > 1M) valor = 1M;

								demanda30 = valor.ToString("0.000");
							}
						}
						<td class="align-right tiny-row">@demanda30</td>
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.proy_vtas_30, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.proy_vtas_60, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>
						<td class="align-right tiny-row">@GridHelper.FormatearDato(item.proy_vtas_90, GridHelper.FormatDato.Monto, item.PermiteDecimales)</td>
						@{
							string claseStk;

							var plazo = item.ctap_rp_plazo_entrega;
							var dias = item.stk_dias;

							if (dias >= plazo * 1.15M)
							{
								claseStk = "bg-stk-verde";
							}
							else if (dias >= plazo)
							{
								claseStk = "bg-stk-azul";
							}
							else
							{
								claseStk = "bg-stk-rojo";
							}
						}
						<td class="align-center tiny-row @claseStk">
							@dias
						</td>
						<td style="display:none;">@item.stk_suc_comprometido</td>
						<td class="align-right tiny-row">@item.p_unidad_pres</td>
						<td class="align-right tiny-row">@item.p_unidad_palet</td>
						<td style="width:5%;"
							class="columna-bulto @Model.ListaDatosProductos.ObtenerClase(item.pedido)
								   @(item.pedido > 0 && item.pedido_tipo == "M" ? "bg-pedido-m" : "")
								   @(item.pedido > 0 && item.pedido_tipo == "A" ? "bg-pedido-a" : "")">
							<div class="input-container">
								<input type="text" class="form-control form-control-sm input-bulto input-numeric"
									   value="@(item.pedido)"
									   data-field="pedido"
									   data-id="@item.p_id"
									   data-original-value="@(item.pedido)" />
							</div>
						</td>

						@if (item.pedido == 0)
						{
							<td class="align-right tiny-row">@item.cantidad</td>
							<td class="align-right tiny-row">
								@GridHelper.FormatearDato(item.costo, GridHelper.FormatDato.Monto, true)
							</td>
							<td class="align-right tiny-row" style="font-weight:bold;">
								@GridHelper.FormatearDato(item.costo_total, GridHelper.FormatDato.Monto, true)
							</td>
						}
						else
						{
							if (item.pedido_tipo == "M")
							{
								<td class="align-right tiny-row" style="background-color: lightgreen;">@item.cantidad</td>
								<td class="align-right tiny-row" style="background-color: lightgreen;">@GridHelper.FormatearDato(item.costo, GridHelper.FormatDato.Monto, true)</td>
								<td class="align-right tiny-row" style="background-color: lightgreen;font-weight:bold;">@GridHelper.FormatearDato(item.costo_total, GridHelper.FormatDato.Monto, true)</td>
							}
							else
							{
								<td class="align-right tiny-row" style="background-color: #6cc6f3;">@item.cantidad</td>
								<td class="align-right tiny-row" style="background-color: #6cc6f3;">@GridHelper.FormatearDato(item.costo, GridHelper.FormatDato.Monto, true)</td>
								<td class="align-right tiny-row" style="background-color: #6cc6f3;font-weight:bold;">@GridHelper.FormatearDato(item.costo_total, GridHelper.FormatDato.Monto, true)</td>
							}
						}
						<td class="align-right tiny-row">@item.paletizado</td>
						<td style="display:none;">@item.pg_id</td>
						<td style="display:none;">@item.rub_id</td>
					</tr>
				}
				
				totalCosto = productos.Sum(x => x.costo_total);
				totalPallet = productos.Sum(x => x.paletizado);
			}
		</tbody>
		<tfoot>
			<tr class="table-secondary fw-bold total-row">
				<td colspan="19" class="text-end">Total:</td>

				<td class="align-right tiny-row" style="font-weight:bold;">
					@GridHelper.FormatearDato(totalCosto, GridHelper.FormatDato.Monto, true)
				</td>
				<td class="align-right tiny-row" style="font-weight:bold;">
					@totalPallet
				</td>

				<td style="display:none;"></td>
				<td style="display:none;"></td>
			</tr>
		</tfoot>
	</table>
</div>
<script>
	$(function() {
			// Esta función aplica los indicadores visuales a los campos modificados en la carga inicial
			$('.campo-modificado').each(function() {
				// Solo si no tiene ya un indicador
				if ($(this).parent('.input-container').find('.indicador-cambio').length === 0) {
					$(this).parent('.input-container').append('<div class="indicador-cambio"></div>');
				}
			});
		});
</script>

@section scripts {
	<script type="text/javascript">
		var pedido = "";
		var pIdEditado = "";
		var rowIndex = 0;
	</script>
}