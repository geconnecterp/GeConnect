@model GridCoreSmart<AConsolidarConteosDto>
@{
}

<div class="table-responsive table-wrapper-400">
    <table class="table table-sm table-hover mb-0 table-golden" id="tbConteos">
        <thead class="sticky-top table-golden-header-compact">
            <tr class="header">
                <th>Código</th>
                <th>Descripción</th>
                <th>Pedido</th>
                <th>a Enviar</th>
                <th>Ctrl. Salida</th>
                <th>Dif.</th>
            </tr>
        </thead>

        <tbody>
            @if (Model == null || Model.ListaDatos == null || !Model.ListaDatos.Any())
            {
                <tr class="fila-vacia">
                    <td colspan="6" class="text-center text-muted py-2">
                        <i class="bx bx-info-circle me-1"></i>
                        No hay conteos para mostrar.
                    </td>
                </tr>
            }
            else
            {
                foreach (var item in Model.ListaDatos)
                {
                    <tr class="row-conteo"
                        data-or-compte="@item.or_compte"
                        data-p-id="@item.p_id">
                        <td>@item.p_id</td>
                        <td>@item.p_desc</td>
                        <td class="text-end">@item.pcd_pedida</td>
                        <td class="text-end">@item.cantidad</td>
                        <td class="text-end">@item.cantidad_ctl</td>
                        <td>@item.estado</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<script>
    $("#tbConteos tbody tr.row-conteo").on("click", function () {
        const conteoId = $(this).data("conteo-id");
        $(document).trigger("rowSelectedConteo", conteoId);
    });
</script>
