@{
    Layout = "~/views/shared/_Layout.cshtml";
}

<div class="grid-golden">
    <div class="grid-golden-body">
        <div class="row align-items-md-start">
            <div class="col-md-8 col-sm-6">
                <div id="gridOfertaNoActivas">
                    <p class="text-center text-muted py-4">
                        <i class="bx bx-info-circle me-2"></i>
                        No hay productos seleccionados para ofertas
                    </p>
                </div>
            </div>
            <div class="col-md-4 col-sm-6">
                <div id="gridCanales"></div>
                <div class="filter-golden">
                    <div class="filter-golden-header">
                        <h5 class="mb-0">Opciones</h5>
                    </div>
                    <div class="filter-golden-body">

                        <button type="button" class="btn btn-primary w-100 mb-2" id="btnActivarOfertas">
                            <i class="bx bx-check-double"></i> Activar Ofertas Seleccionadas
                        </button>
                        <button type="button" class="btn btn-outline-danger w-100 mb-2" id="btnEliminarSelec">
                            <i class="bx bx-trash me-1"></i> Eliminar Seleccionados
                        </button>
                        <button type="button" class="btn btn-outline-primary w-100 mb-2" id="btnActivarVencimiento">
                            <i class="bx bx-calendar-alt me-1"></i> Activar Vencimiento
                        </button>
                        <button type="button" class="btn btn-outline-primary w-100 mb-2" id="btnCargarActivos">
                            <i class="bx bxs-cart me-1"></i> Cargar Activos
                        </button>
                        <button type="button" class="btn btn-outline-secondary w-100 mb-2" id="btnImprimir">
                            <i class="bx bx-printer me-1"></i> Imprimir
                        </button>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script type="text/javascript">
        let buscarCanalesUrl = "@Url.Action("BuscarCanales", "Ofertas", new { area = "Productos" })";
        let presentarOfertasSinActivarUrl = "@Url.Action("PresentarOfertasSinActivar", "OfertasActivar", new { area = "Productos" })";
        let activarOfertaUrl =  "@Url.Action("ActivarOferta", "OfertasActivar", new { area = "Productos" })";
        let actualizarOfertaVencidaSinActivarUrl = "@Url.Action("ActualizarOfertaVencidaSinActivar", "OfertasActivar", new { area = "Productos" })";
        let cargarActivosASinActivarUrl = "@Url.Action("CargarActivosASinActivar", "OfertasActivar", new { area = "Productos" })";
        let eliminarOfertasSinActivarUrl = "@Url.Action("EliminarOfertasSinActivar", "OfertasActivar", new { area = "Productos" })";

        let homeOfertaUrl = "@Url.Action("index", "OfertasActivar", new { area = "Productos" })";
    </script>
    <script src="~/js/app/areas/precios/ofertaActivar.js" type="text/javascript"></script>

}