@using gc.infraestructura.Dtos.Productos.Ofertas
@model gc.infraestructura.Dtos.Gen.GridCoreSmart<ComboSustitutoDto>

<div class="grid-golden h-100">
    <div class="grid-golden-header promo-subgrid-header">
        <h6 class="mb-0">Sustitutos</h6>
        <button type="button" class="btn btn-sm btn-outline-light promo-subgrid-action" id="btnAgregarSustituto" title="Agregar Sustituto" disabled>
            <i class="bx bx-plus"></i>
        </button>
    </div>
    <div class="grid-golden-body p-1">
        <div class="table-responsive" style="max-height: 300px;">
            <table class="table table-sm table-hover mb-0 table-golden" id="tbGridSustitutos">
                <thead class="sticky-top table-golden-header-compact">
                    <tr class="header">
                        <th class="text-center th-compact">ID</th>
                        <th class="text-left th-compact">Descripción</th>
                        <th class="text-center th-compact">Costo</th>
                        @* <th class="text-center th-compact">Estado</th> *@
                        <th class="text-center th-compact">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model?.ListaDatos?.Any() == true)
                    {
                        foreach (var item in Model.ListaDatos)
                        {
                            <tr class="@(item.activo == 'D' ? "table-secondary text-muted" : "")"
                                data-producto-id="@item.p_id_sustituto" data-producto-padre-id="@item.p_id"
                                data-combo-id="@item.cmb_id" data-producto-estado="@item.activo">
                                <td class="text-center">
                                    @item.p_id_sustituto
                                </td>
                                <td class="promo-descripcion" title="@item.p_desc">
                                    @item.p_desc
                                    @if (item.activo == 'D')
                                    {
                                        <span class="badge bg-secondary ms-1">Desactivado</span>
                                    }
                                </td>
                                <td class="text-end">
                                    @item.p_pcosto.ToString("N3", System.Globalization.CultureInfo.GetCultureInfo("en-US"))
                                </td>
                                @* <td class="text-center">
                                    <span class="badge @(item.activo == 'S' ? "bg-success" : "bg-danger")">
                                        @(item.activo == 'S' ? "Activo" : "Inactivo")
                                    </span>
                                </td> *@
                                <td class="text-center promo-accion">
                                    @if (item.activo == 'D')
                                    {
                                        <button type="button" class="btn btn-sm btn-outline-secondary" title="Sustituto desactivado" disabled>
                                            <i class="bx bx-block"></i>
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="button" class="btn btn-sm btn-outline-danger btn-eliminar-sustituto"
                                                title="Eliminar sustituto" data-producto-id="@item.p_id_sustituto">
                                            <i class="bx bx-trash"></i>
                                        </button>
                                    }
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center text-muted py-2">
                                <i class="bx bx-info-circle me-1"></i>No hay sustitutos disponibles
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
