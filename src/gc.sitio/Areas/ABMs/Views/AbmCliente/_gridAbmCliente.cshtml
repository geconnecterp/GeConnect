@model GridCoreSmart<ABMClienteSearchDto>

@{
    var dataMod = new ABMClienteSearchDto();
    var alt = true;
    string clase = string.Empty;
    string sortDirInv = Model.SortDir.Equals("ASC") ? "DESC" : "ASC";
}
<style>
    thead th {
        position: sticky;
        top: 0px;
    }
</style>
<div class="card">
    <div class="card-body">
        <div class="table-responsive text-nowrap  table-wrapper">
            <table class="table table-sm mb-0 table-hover table-wrapper-fixed-head" id="tbGridCliente">
                <thead class="table-dark">
                    <tr class="header">
                        <th scope="col" class="align-center">ID</th>
                        <th scope="col" class="align-center">Denominación</th>
                        <th scope="col" class="align-center">Doc. Tipo</th>
                        <th scope="col" class="align-center">Doc. Nro.</th>
                        <th scope="col" class="align-center">Domicilio</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ListaDatos)
                    {
                        if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }

                        <tr class="@clase" onclick="selectReg(this, 'tbGridCliente')" 
                        @* ondblclick="selectRegDbl(this, 'tbGridCliente')" *@
                        >
                            <td class="text-sm-center">@item.cta_id</td>
                            <td class="text-sm-start">@item.cta_denominacion</td>
                            <td class="text-sm-center">@item.tdoc_desc</td>
                            <td class="text-sm-center">@item.cta_documento</td>
                            <td class="text-sm-start">@item.cta_domicilio</td>
                        </tr>
                    }
                    
                </tbody>
            </table>           
        </div>
    </div>
</div>