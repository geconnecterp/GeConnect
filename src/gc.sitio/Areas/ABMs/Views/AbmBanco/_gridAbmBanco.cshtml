@model GridCore<ABMBancoSearchDto>
@{
    var dataMod = new ABMBancoSearchDto();
    var alt = true;
    string clase = string.Empty;
    string sortDirInv = Model.SortDir.Equals("ASC") ? "DESC" : "ASC";
}
<style>
    thead th {
        position: sticky;
        top: 0px;
    }
</style>
<div class="card">
    <div class="card-body">
        <div class="table-responsive text-nowrap  table-wrapper">
            <table class="table table-sm mb-0 table-hover table-wrapper-fixed-head" id="tbGridBanco">
                <thead class="table-dark">
                    <tr class="header">
                        <th scope="col">ID</th>
                        <th scope="col">Banco</th>
                        <th scope="col">Tipo Cuenta</th>
                        <th scope="col">N° Cuenta</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ListaDatos)
                    {
                        if (alt) { clase = "alt"; alt = false; } else { clase = string.Empty; alt = true; }

                        <tr class="@clase" onclick="selectReg(this, 'tbGridBanco')">
                            <td class="text-sm-center">@item.Ctaf_Id</td>
                            <td class="text-sm-start">@item.Ban_Razon_Social.ToUpper()</td>
                            <td class="text-sm-start">@item.Tcb_Desc.ToUpper()</td>
                            <td class="text-sm-start">@item.Ban_Cuenta_Nro</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>